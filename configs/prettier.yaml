extends:
  - ./default.json
  - prettier
  - prettier/react

plugins:
  - prettier

rules:
  prettier/prettier:
    - error
    - bracketSpacing: true
      printWidth: 120
      singleQuote: true
      tabWidth: 2
