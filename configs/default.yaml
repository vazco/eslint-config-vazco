parser: babel-eslint

extends:
  - prettier
  - ./base

plugins:
  - babel

rules:
  prettier/prettier:
    - error
    - arrowParens: avoid
      bracketSpacing: true
      printWidth: 80
      singleQuote: true
      tabWidth: 2
      trailingComma: none
